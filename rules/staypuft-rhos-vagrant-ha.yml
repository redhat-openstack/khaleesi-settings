---
args:
    installer: staypuft
    installer-topology: vagrant
    installer-deployment: ha_controller_ceph_backend
    installer-network: neutron
    installer-network-variant: gre
    product: rhos
    product-version: 5.0
    job: staypuft
    provisioner: vagrant
    product-version-build: latest

validation:
    must_have:
        - distro
        - product-version-repo
