--- !extends:common.yml
# if you touch this, don't forget to augment
# settings/provisioner/common-bm.yml hotfix
tester:
    type: tempest
    dir: tempest-dir
    image_id: !lookup provisioner.images.rhel.7<DOT_ANCHOR>1.id
    remote_user: !lookup provisioner.images.rhel.7<DOT_ANCHOR>1.remote_user
    images:
        cirros:
            url:
            id: cirros-0.3.1-x86_64-disk.img

logs:
    - 'etc/tempest.conf'
    - 'etc/tempest.conf.sample'
    - '*.log'
    - '*.xml'
