---
tester:
    component: horizon
    dir: horizon-dir
    git:
        repo: git://REPLACE_ME/users/mpavlase/horizon
        # This line below will not work, because !lookup is not processed as macro
        # Name of branch is composed on khaleesi side.
        #revision: !join [!lookup product.name, '-', !lookup product.version]

