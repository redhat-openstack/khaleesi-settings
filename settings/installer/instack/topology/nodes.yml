nodes:
    controller:
        name: controller
        remote_user: heat-admin
        scale: 1

    tester:
        name: tempest
        remote_user: root

    compute:
        name: compute
        type: Compute

    blockstorage:
        name: blockstorage
        type: Cinder-Storage

    swiftstorage:
        name: swiftstorage
        type: Swift-Storage

    cephstorage:
        name: cephstorage
        type: Ceph-Storage
