---
provisioner:
    network:
        vlan:
            external:
                tag:
            internal:
                range:
                    start: 290
                    end: 340

base_dir: !env WORKSPACE